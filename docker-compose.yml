services:
  postgres:
    image: postgres:16
    container_name: postgres-english
    ports:
      - "45432:5432"
    environment:
      POSTGRES_DB: english_trainer
      POSTGRES_USER: english
      POSTGRES_PASSWORD: english
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
